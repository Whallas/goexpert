# Weather provider: "openmeteo" (default, no key needed) or "weatherapi".
WEATHER_PROVIDER=openmeteo

# Required only when WEATHER_PROVIDER=weatherapi.
# Get a free key at https://www.weatherapi.com/
WEATHER_API_KEY=

PORT=8080
