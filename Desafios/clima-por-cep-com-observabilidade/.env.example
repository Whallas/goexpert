# Provedor de clima: "openmeteo" (padrão, sem chave) ou "weatherapi".
WEATHER_PROVIDER=openmeteo

# Chave da WeatherAPI (https://www.weatherapi.com/).
# Obrigatória somente quando WEATHER_PROVIDER=weatherapi.
WEATHER_API_KEY=
